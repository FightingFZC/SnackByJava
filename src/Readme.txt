1、Main方法就只负责new一个初始化界面的对象。启动用的

2、GameStartJFrame类负责搞定初始化界面
    1）：一个GameStartJFrame，使用绝对布局
    2）：添加GameStartPanel,前者放游戏名字的图片，后者放按钮来实现功能
    3）：在GameStartPanel上添加四个按钮
        按钮一：点击弹出一个弹窗，附带游戏说明（待完善）
            1.弹出一个弹窗，写着操作说明，与付智超有多牛逼，多帅

        按钮二：游戏开始，画出游戏界面
            1.清除这个GameStartPanel然后添加一个GamingPanel

        按钮三：游戏结束
            1.跳出一个弹窗，点确定和X都是退出JVM虚拟机

        按钮四：弹出历史分数（待完善）
            1.这个需要使用IO流，每次游戏结束把时间和分数写在游戏目录下，这个按钮就是会读取这个文件。

3、GamingJFrame类负责游戏中的界面。
    1、先是初始化，由于没怎么用面向对象的思维写，这个类直接当键盘监听器和动作监听器了
    2、画笔方法用来画变化的东西。如：变化的蛇，变化的食物，每次刷新都要重画一次
    3、什么都不干的情况下就沿着一个方向走。
    4、虽说蛇的方向会改变，但只要改变头就行。
    5、蛇身体是如何变化的呢？
        1.从末尾开始，每次等于身体的前一块，然后头再往自己的方向前进一块
        2.for(int i = length - 1; i > 0; i--){
                          snackX[i] = snackX[i - 1];
                          snackY[i] = snackY[i - 1];
                      }
        3.全靠这一个循环使得蛇的身体变化与增加非常简单
        4.从末尾开始，要是吃了个东西增加了身体，刚好在刷新前进的时候代替身体的末尾
        5.这个太妙了。